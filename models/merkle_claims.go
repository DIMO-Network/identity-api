// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// MerkleClaim is an object representing the database table.
type MerkleClaim struct {
	PoolID    int           `boil:"pool_id" json:"pool_id" toml:"pool_id" yaml:"pool_id"`
	Epoch     int           `boil:"epoch" json:"epoch" toml:"epoch" yaml:"epoch"`
	Account   []byte        `boil:"account" json:"account" toml:"account" yaml:"account"`
	Amount    types.Decimal `boil:"amount" json:"amount" toml:"amount" yaml:"amount"`
	Proof     types.JSON    `boil:"proof" json:"proof" toml:"proof" yaml:"proof"`
	ClaimedAt null.Time     `boil:"claimed_at" json:"claimed_at,omitempty" toml:"claimed_at" yaml:"claimed_at,omitempty"`
	ClaimTX   null.Bytes    `boil:"claim_tx" json:"claim_tx,omitempty" toml:"claim_tx" yaml:"claim_tx,omitempty"`

	R *merkleClaimR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L merkleClaimL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MerkleClaimColumns = struct {
	PoolID    string
	Epoch     string
	Account   string
	Amount    string
	Proof     string
	ClaimedAt string
	ClaimTX   string
}{
	PoolID:    "pool_id",
	Epoch:     "epoch",
	Account:   "account",
	Amount:    "amount",
	Proof:     "proof",
	ClaimedAt: "claimed_at",
	ClaimTX:   "claim_tx",
}

var MerkleClaimTableColumns = struct {
	PoolID    string
	Epoch     string
	Account   string
	Amount    string
	Proof     string
	ClaimedAt string
	ClaimTX   string
}{
	PoolID:    "merkle_claims.pool_id",
	Epoch:     "merkle_claims.epoch",
	Account:   "merkle_claims.account",
	Amount:    "merkle_claims.amount",
	Proof:     "merkle_claims.proof",
	ClaimedAt: "merkle_claims.claimed_at",
	ClaimTX:   "merkle_claims.claim_tx",
}

// Generated where

type whereHelpertypes_Decimal struct{ field string }

func (w whereHelpertypes_Decimal) EQ(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertypes_Decimal) NEQ(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertypes_Decimal) LT(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertypes_Decimal) LTE(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertypes_Decimal) GT(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertypes_Decimal) GTE(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

type whereHelpertypes_JSON struct{ field string }

func (w whereHelpertypes_JSON) EQ(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertypes_JSON) NEQ(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertypes_JSON) LT(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertypes_JSON) LTE(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertypes_JSON) GT(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertypes_JSON) GTE(x types.JSON) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var MerkleClaimWhere = struct {
	PoolID    whereHelperint
	Epoch     whereHelperint
	Account   whereHelper__byte
	Amount    whereHelpertypes_Decimal
	Proof     whereHelpertypes_JSON
	ClaimedAt whereHelpernull_Time
	ClaimTX   whereHelpernull_Bytes
}{
	PoolID:    whereHelperint{field: "\"identity_api\".\"merkle_claims\".\"pool_id\""},
	Epoch:     whereHelperint{field: "\"identity_api\".\"merkle_claims\".\"epoch\""},
	Account:   whereHelper__byte{field: "\"identity_api\".\"merkle_claims\".\"account\""},
	Amount:    whereHelpertypes_Decimal{field: "\"identity_api\".\"merkle_claims\".\"amount\""},
	Proof:     whereHelpertypes_JSON{field: "\"identity_api\".\"merkle_claims\".\"proof\""},
	ClaimedAt: whereHelpernull_Time{field: "\"identity_api\".\"merkle_claims\".\"claimed_at\""},
	ClaimTX:   whereHelpernull_Bytes{field: "\"identity_api\".\"merkle_claims\".\"claim_tx\""},
}

// MerkleClaimRels is where relationship names are stored.
var MerkleClaimRels = struct {
}{}

// merkleClaimR is where relationships are stored.
type merkleClaimR struct {
}

// NewStruct creates a new relationship struct
func (*merkleClaimR) NewStruct() *merkleClaimR {
	return &merkleClaimR{}
}

// merkleClaimL is where Load methods for each relationship are stored.
type merkleClaimL struct{}

var (
	merkleClaimAllColumns            = []string{"pool_id", "epoch", "account", "amount", "proof", "claimed_at", "claim_tx"}
	merkleClaimColumnsWithoutDefault = []string{"pool_id", "epoch", "account", "amount", "proof"}
	merkleClaimColumnsWithDefault    = []string{"claimed_at", "claim_tx"}
	merkleClaimPrimaryKeyColumns     = []string{"pool_id", "epoch", "account"}
	merkleClaimGeneratedColumns      = []string{}
)

type (
	// MerkleClaimSlice is an alias for a slice of pointers to MerkleClaim.
	// This should almost always be used instead of []MerkleClaim.
	MerkleClaimSlice []*MerkleClaim
	// MerkleClaimHook is the signature for custom MerkleClaim hook methods
	MerkleClaimHook func(context.Context, boil.ContextExecutor, *MerkleClaim) error

	merkleClaimQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	merkleClaimType                 = reflect.TypeOf(&MerkleClaim{})
	merkleClaimMapping              = queries.MakeStructMapping(merkleClaimType)
	merkleClaimPrimaryKeyMapping, _ = queries.BindMapping(merkleClaimType, merkleClaimMapping, merkleClaimPrimaryKeyColumns)
	merkleClaimInsertCacheMut       sync.RWMutex
	merkleClaimInsertCache          = make(map[string]insertCache)
	merkleClaimUpdateCacheMut       sync.RWMutex
	merkleClaimUpdateCache          = make(map[string]updateCache)
	merkleClaimUpsertCacheMut       sync.RWMutex
	merkleClaimUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var merkleClaimAfterSelectMu sync.Mutex
var merkleClaimAfterSelectHooks []MerkleClaimHook

var merkleClaimBeforeInsertMu sync.Mutex
var merkleClaimBeforeInsertHooks []MerkleClaimHook
var merkleClaimAfterInsertMu sync.Mutex
var merkleClaimAfterInsertHooks []MerkleClaimHook

var merkleClaimBeforeUpdateMu sync.Mutex
var merkleClaimBeforeUpdateHooks []MerkleClaimHook
var merkleClaimAfterUpdateMu sync.Mutex
var merkleClaimAfterUpdateHooks []MerkleClaimHook

var merkleClaimBeforeDeleteMu sync.Mutex
var merkleClaimBeforeDeleteHooks []MerkleClaimHook
var merkleClaimAfterDeleteMu sync.Mutex
var merkleClaimAfterDeleteHooks []MerkleClaimHook

var merkleClaimBeforeUpsertMu sync.Mutex
var merkleClaimBeforeUpsertHooks []MerkleClaimHook
var merkleClaimAfterUpsertMu sync.Mutex
var merkleClaimAfterUpsertHooks []MerkleClaimHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MerkleClaim) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range merkleClaimAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MerkleClaim) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range merkleClaimBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MerkleClaim) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range merkleClaimAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MerkleClaim) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range merkleClaimBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MerkleClaim) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range merkleClaimAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MerkleClaim) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range merkleClaimBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MerkleClaim) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range merkleClaimAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MerkleClaim) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range merkleClaimBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MerkleClaim) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range merkleClaimAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMerkleClaimHook registers your hook function for all future operations.
func AddMerkleClaimHook(hookPoint boil.HookPoint, merkleClaimHook MerkleClaimHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		merkleClaimAfterSelectMu.Lock()
		merkleClaimAfterSelectHooks = append(merkleClaimAfterSelectHooks, merkleClaimHook)
		merkleClaimAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		merkleClaimBeforeInsertMu.Lock()
		merkleClaimBeforeInsertHooks = append(merkleClaimBeforeInsertHooks, merkleClaimHook)
		merkleClaimBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		merkleClaimAfterInsertMu.Lock()
		merkleClaimAfterInsertHooks = append(merkleClaimAfterInsertHooks, merkleClaimHook)
		merkleClaimAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		merkleClaimBeforeUpdateMu.Lock()
		merkleClaimBeforeUpdateHooks = append(merkleClaimBeforeUpdateHooks, merkleClaimHook)
		merkleClaimBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		merkleClaimAfterUpdateMu.Lock()
		merkleClaimAfterUpdateHooks = append(merkleClaimAfterUpdateHooks, merkleClaimHook)
		merkleClaimAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		merkleClaimBeforeDeleteMu.Lock()
		merkleClaimBeforeDeleteHooks = append(merkleClaimBeforeDeleteHooks, merkleClaimHook)
		merkleClaimBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		merkleClaimAfterDeleteMu.Lock()
		merkleClaimAfterDeleteHooks = append(merkleClaimAfterDeleteHooks, merkleClaimHook)
		merkleClaimAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		merkleClaimBeforeUpsertMu.Lock()
		merkleClaimBeforeUpsertHooks = append(merkleClaimBeforeUpsertHooks, merkleClaimHook)
		merkleClaimBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		merkleClaimAfterUpsertMu.Lock()
		merkleClaimAfterUpsertHooks = append(merkleClaimAfterUpsertHooks, merkleClaimHook)
		merkleClaimAfterUpsertMu.Unlock()
	}
}

// One returns a single merkleClaim record from the query.
func (q merkleClaimQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MerkleClaim, error) {
	o := &MerkleClaim{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for merkle_claims")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all MerkleClaim records from the query.
func (q merkleClaimQuery) All(ctx context.Context, exec boil.ContextExecutor) (MerkleClaimSlice, error) {
	var o []*MerkleClaim

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to MerkleClaim slice")
	}

	if len(merkleClaimAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all MerkleClaim records in the query.
func (q merkleClaimQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count merkle_claims rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q merkleClaimQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if merkle_claims exists")
	}

	return count > 0, nil
}

// MerkleClaims retrieves all the records using an executor.
func MerkleClaims(mods ...qm.QueryMod) merkleClaimQuery {
	mods = append(mods, qm.From("\"identity_api\".\"merkle_claims\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"identity_api\".\"merkle_claims\".*"})
	}

	return merkleClaimQuery{q}
}

// FindMerkleClaim retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMerkleClaim(ctx context.Context, exec boil.ContextExecutor, poolID int, epoch int, account []byte, selectCols ...string) (*MerkleClaim, error) {
	merkleClaimObj := &MerkleClaim{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"identity_api\".\"merkle_claims\" where \"pool_id\"=$1 AND \"epoch\"=$2 AND \"account\"=$3", sel,
	)

	q := queries.Raw(query, poolID, epoch, account)

	err := q.Bind(ctx, exec, merkleClaimObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from merkle_claims")
	}

	if err = merkleClaimObj.doAfterSelectHooks(ctx, exec); err != nil {
		return merkleClaimObj, err
	}

	return merkleClaimObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MerkleClaim) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no merkle_claims provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(merkleClaimColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	merkleClaimInsertCacheMut.RLock()
	cache, cached := merkleClaimInsertCache[key]
	merkleClaimInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			merkleClaimAllColumns,
			merkleClaimColumnsWithDefault,
			merkleClaimColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(merkleClaimType, merkleClaimMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(merkleClaimType, merkleClaimMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"identity_api\".\"merkle_claims\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"identity_api\".\"merkle_claims\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into merkle_claims")
	}

	if !cached {
		merkleClaimInsertCacheMut.Lock()
		merkleClaimInsertCache[key] = cache
		merkleClaimInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the MerkleClaim.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MerkleClaim) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	merkleClaimUpdateCacheMut.RLock()
	cache, cached := merkleClaimUpdateCache[key]
	merkleClaimUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			merkleClaimAllColumns,
			merkleClaimPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update merkle_claims, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"identity_api\".\"merkle_claims\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, merkleClaimPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(merkleClaimType, merkleClaimMapping, append(wl, merkleClaimPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update merkle_claims row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for merkle_claims")
	}

	if !cached {
		merkleClaimUpdateCacheMut.Lock()
		merkleClaimUpdateCache[key] = cache
		merkleClaimUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q merkleClaimQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for merkle_claims")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for merkle_claims")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MerkleClaimSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), merkleClaimPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"identity_api\".\"merkle_claims\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, merkleClaimPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in merkleClaim slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all merkleClaim")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MerkleClaim) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no merkle_claims provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(merkleClaimColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	merkleClaimUpsertCacheMut.RLock()
	cache, cached := merkleClaimUpsertCache[key]
	merkleClaimUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			merkleClaimAllColumns,
			merkleClaimColumnsWithDefault,
			merkleClaimColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			merkleClaimAllColumns,
			merkleClaimPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert merkle_claims, could not build update column list")
		}

		ret := strmangle.SetComplement(merkleClaimAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(merkleClaimPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert merkle_claims, could not build conflict column list")
			}

			conflict = make([]string, len(merkleClaimPrimaryKeyColumns))
			copy(conflict, merkleClaimPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"identity_api\".\"merkle_claims\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(merkleClaimType, merkleClaimMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(merkleClaimType, merkleClaimMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert merkle_claims")
	}

	if !cached {
		merkleClaimUpsertCacheMut.Lock()
		merkleClaimUpsertCache[key] = cache
		merkleClaimUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single MerkleClaim record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MerkleClaim) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no MerkleClaim provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), merkleClaimPrimaryKeyMapping)
	sql := "DELETE FROM \"identity_api\".\"merkle_claims\" WHERE \"pool_id\"=$1 AND \"epoch\"=$2 AND \"account\"=$3"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from merkle_claims")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for merkle_claims")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q merkleClaimQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no merkleClaimQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from merkle_claims")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for merkle_claims")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MerkleClaimSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(merkleClaimBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), merkleClaimPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"identity_api\".\"merkle_claims\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, merkleClaimPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from merkleClaim slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for merkle_claims")
	}

	if len(merkleClaimAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MerkleClaim) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMerkleClaim(ctx, exec, o.PoolID, o.Epoch, o.Account)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MerkleClaimSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MerkleClaimSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), merkleClaimPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"identity_api\".\"merkle_claims\".* FROM \"identity_api\".\"merkle_claims\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, merkleClaimPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in MerkleClaimSlice")
	}

	*o = slice

	return nil
}

// MerkleClaimExists checks if the MerkleClaim row exists.
func MerkleClaimExists(ctx context.Context, exec boil.ContextExecutor, poolID int, epoch int, account []byte) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"identity_api\".\"merkle_claims\" where \"pool_id\"=$1 AND \"epoch\"=$2 AND \"account\"=$3 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, poolID, epoch, account)
	}
	row := exec.QueryRowContext(ctx, sql, poolID, epoch, account)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if merkle_claims exists")
	}

	return exists, nil
}

// Exists checks if the MerkleClaim row exists.
func (o *MerkleClaim) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return MerkleClaimExists(ctx, exec, o.PoolID, o.Epoch, o.Account)
}
